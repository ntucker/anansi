{
  "compilerOptions": {
    "types": ["@anansi/webpack-config/types"<% if (reactMode !== 'legacy') { %>, "react-dom/next", "react/next"<% } %><% if (features.includes('testing')) {%>, "jest"<% } %>],
    "paths": {
      "resources/*": ["resources/*"]
    },
    "noEmit": true
  }
}
