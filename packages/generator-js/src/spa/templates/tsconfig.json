{
  "compilerOptions": {
<% if (reactMode !== 'legacy') { %>
  "types": ["react-dom/experimental", "react/experimental"<% if (features.includes('testing')) {%>, "jest"<% } %>],
<% } %>
    "noEmit": true
  }
}
