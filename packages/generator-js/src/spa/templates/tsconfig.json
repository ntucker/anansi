{
  "compilerOptions": {
    "moduleResolution": "bundler",
    "types": ["@anansi/webpack-config/types"<% if (features.includes('testing')) {%>, "jest"<% } %>],
    "paths": {
      "api/*": ["api/*"]
    },
    "noEmit": true
  }
}
